import React, { FC } from 'react';
import About from './_sections/About';
import Hero from './_sections/Hero';

const Home: FC = () => (
  <main>
    <Hero />
    <About />
  </main>
);

export default Home;
