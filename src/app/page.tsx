import About from '@/components/About';
import Hero from '@/components/Hero';
import React, { FC } from 'react';

const Home: FC = () => (
  <main>
    <Hero />
    <About />
  </main>
);

export default Home;
